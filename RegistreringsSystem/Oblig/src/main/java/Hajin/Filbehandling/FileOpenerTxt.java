package Hajin.Filbehandling;

public class FileOpenerTxt implements FileOpener{

}
//public class Read {
    /*public static ObservableList<Person> readPeople(String path) throws IOException {
        ArrayList<Person> list = new ArrayList<>();

        try (BufferedReader reader = Files.newBufferedReader(Paths.get(path))) {
            String line;

            while((line = reader.readLine()) != null) {
                Person p = ParsePerson.parsePerson(line);
                list.add(p);
            }
        }

        return (ObservableList<Person>) list;
    }
}

//Velge et bestemt sted å åpne filer fra setInitialDirectory(new File("navn på adressen"));*/
